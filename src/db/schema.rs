table! {
    events (event_id) {
        event_id -> Integer,
        event_date -> Text,
        event_desc -> Text,
        event_from -> Text,
        event_to -> Text,
        event_acc -> Text,
        event_sum -> Integer,
    }
}
